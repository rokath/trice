# -----------------------------------------------------------------------------
# IntelliJ / CLion project files (.idea)
#
# Goal:
# - Keep only stable, shareable project metadata in Git.
# - Ignore user-specific / machine-specific state (workspace, tasks, shelves, ...).
# - Allow a shared spelling dictionary for the project.
# -----------------------------------------------------------------------------

# --- User-specific IDE state (never commit) -----------------------------------
/workspace.xml              # Window layout, opened files, run/debug selections, etc.
/tasks.xml                  # Task management state (user-specific)
/usage.statistics.xml       # IDE usage stats (user-specific)
/shelf/                     # Shelved changes (user-specific, can contain patches)

# --- Project metadata (OK to commit) ------------------------------------------
/misc.xml                   # Project SDK/toolchain/language level and general settings
/modules.xml                # Module list (project structure)
/trice.iml                  # Module file generated by CLion/IntelliJ
/vcs.xml                    # VCS mappings (e.g., Git root)

# --- Shared spelling dictionary (commit this) ---------------------------------
/trice.dic                  # Custom spelling dictionary (one word per line)

# --- Optional: ignore caches and transient folders ----------------------------
/caches/                    # IDE caches (if present)
/libraries/                 # Auto-generated library definitions (often not needed)
/artifacts/                 # Build artifacts metadata (rarely needed)
